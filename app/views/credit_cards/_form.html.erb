<%= form_with(model: credit_card, local: true) do |form| %>
  <% if credit_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(credit_card.errors.count, "error") %> prohibited this credit_card from being saved:</h2>

      <ul>
      <% credit_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number, id: :credit_card_number %>
  </div>

  <div class="field">
    <%= form.label :cvc %>
    <%= form.text_field :cvc, id: :credit_card_cvc %>
  </div>

  <div class="field">
    <%= form.label :digits %>
    <%= form.text_field :digits, id: :credit_card_digits %>
  </div>

  <div class="field">
    <%= form.label :month %>
    <%= form.text_field :month, id: :credit_card_month %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.text_field :year, id: :credit_card_year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
